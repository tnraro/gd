{
  "name": "gd",
  "module": "src/index.ts",
  "type": "module",
  "devDependencies": {
    "@types/bun": "latest",
    "mitata": "^1.0.34"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "scripts": {
    "dev": "bun run src/index.ts",
    "build": "bun build src/index.ts --outdir out --target bun --minify --format cjs --bytecode",
    "preview": "bun run out/index.js"
  },
  "dependencies": {
    "zstd.ts": "^1.1.3"
  }
}
